---
description: Domain implementation patterns for actions, widgets, and features
alwaysApply: true
---

# Domain Patterns

Prefer consistent domain-first implementation across `actions`, `features`, and
`widgets`.

## Typical implementation flow

1. Check `@/codegen/api/product` for generated types, schemas, query options,
   mutation options, and hooks.
2. Wrap generated network hooks in `src/actions/<domain>/hooks/use-<name>.ts`.
3. Build reusable presentational UI in `src/features/<domain>/`.
4. Compose behavior and UI in `src/widgets/<domain>/`.
5. Wire page-level layout in `src/entrypoints/<name>/component.tsx`.
6. Reference the entrypoint from `src/routes/**`.

## Action hook pattern

```typescript
import { useGetApiV1Templates } from "@/codegen/api/product";

export function useGetTemplates() {
  const {
    data: templatesData,
    isLoading: isTemplatesLoading,
    isError: isTemplatesError,
  } = useGetApiV1Templates();

  return { templatesData, isTemplatesLoading, isTemplatesError };
}
```

GET action hooks expose TanStack Query state. Do not wrap them in `try/catch`;
components/widgets should render from `data`, loading, and error flags.

Mutation action hooks own common callbacks such as cache invalidation, metrics,
and generic toasts. Widget hooks may pass per-call `mutate(..., { onSuccess,
onError })` callbacks for UI-specific side effects. Do not use `mutateAsync`
plus `try/catch` for ordinary UI flows.

## Widget hook pattern

```typescript
import { useNavigate, useSearch } from "@tanstack/react-router";

import { useGetTemplates } from "@/actions/templates/hooks/use-get-templates";

export function useTemplatesWidget() {
  const navigate = useNavigate();
  const search = useSearch({ from: "/_with-auth/_with-subscription/home" });
  const { templatesData, isTemplatesLoading } = useGetTemplates();

  return { navigate, search, templatesData, isTemplatesLoading };
}
```

Widget components should primarily render. Put navigation, derived values,
handlers, mutation calls, and local state into colocated hooks. If a child
widget component needs logic, split it into its own component file and hook.

## Form pattern

```typescript
import { useAppForm } from "@/lib/tanstack-form";
import { z } from "@/lib/zod";

const schema = z.object({ name: z.string().min(1) });

const form = useAppForm({
  defaultValues: { name: "" },
  validators: { onSubmit: schema },
  onSubmit: async ({ value }) => {
    // call mutation
  },
});
```

Forms are widget-owned. Keep `useAppForm`, `withForm` children, schemas, form
types, and form helpers under the owning widget. Do not move form subcomponents
wrapped in `withForm` into `features`.

## Codegen imports outside actions

Allowed when needed:

- Types and enums.
- Zod schemas derived from API contracts.
- Query keys for invalidation.
- Query options in route guards/preloaders.

Avoid adding new direct generated network hook calls in widgets/features. Prefer
action wrappers.

## Naming and organization

- Domain folder names: `kebab-case`.
- Hook filenames: `use-<name>.ts`.
- Component filenames: kebab-case `.tsx`.
- Co-locate local component hooks with their widget/feature.
- Prefer one meaningful component per file. Skeletons/plugs/empty states may
  stay with the main component; semantic child components should be separate.
- Keep broad constants, utilities, schemas, and shared types out of component
  files.

## Avoid

- Cross-domain coupling inside `shared`.
- Business logic in route files.
- Manual edits to generated files.
- New custom abstractions or composition patterns without user agreement.
- Guessing when local references conflict or the requested behavior is unclear.

## Reference examples

- GET action hook: `src/actions/templates/hooks/use-get-templates.ts`.
- Mutation action hook with callbacks:
  `src/actions/ideas-lists/hooks/use-create-ideas-list.ts`.
- Widget hook + component:
  `src/widgets/scenario/scenario-app-menubar/hooks/use-scenario-app-menubar.ts`
  and
  `src/widgets/scenario/scenario-app-menubar/components/scenario-app-menubar.tsx`.
- Widget-owned form:
  `src/widgets/profile-settings/profile-settings/hooks/use-profile-settings-form.ts`
  plus sibling `profile-settings-form*.tsx` files.
- Presentational feature:
  `src/features/templates/template-card/components/template-card.tsx`.
