---
description: Require analysis of at least 3 similar domain examples before creating new files
alwaysApply: true
---

# Domain Reference Analysis Before New Files

Before creating any new file in `actions`, `features`, `widgets`, or
`entrypoints`, inspect local references first.

## Mandatory pre-coding step

1. Find at least 3 similar implementations in the same layer.
2. Prefer the same domain; otherwise use the closest related domains.
3. Compare file structure, naming, hook/component split, imports, and return
   shapes.
4. If the references conflict, the target composition is ambiguous, or the
   implementation requires a new custom pattern, ask the user before coding.

## What to inspect by layer

| Creating              | Inspect                                    |
| --------------------- | ------------------------------------------ |
| New action hook       | `src/actions/<domain>/hooks/`              |
| New feature component | `src/features/<domain>/`                   |
| New widget            | `src/widgets/<domain>/` or related widgets |
| New entrypoint        | `src/entrypoints/`                         |

## Required output in task flow

- List the 3+ reference paths used.
- Reuse naming, file structure, and import conventions from those references.
- Explain briefly if the new implementation must deviate from local patterns.

Do not write new domain files before completing this reference pass.

## Reference starting points

- Action hooks: `src/actions/templates/hooks/use-get-templates.ts`,
  `src/actions/ideas-lists/hooks/use-create-ideas-list.ts`.
- Widgets:
  `src/widgets/scenario/scenario-app-menubar/components/scenario-app-menubar.tsx`,
  `src/widgets/scenario/scenario-app-menubar/hooks/use-scenario-app-menubar.ts`.
- Widget forms:
  `src/widgets/profile-settings-general/components/profile-settings-general-form.tsx`.
- Features:
  `src/features/profiles/profile-card/components/profile-card.tsx`,
  `src/features/templates/template-card/components/template-card.tsx`.
- Entrypoints: `src/entrypoints/scenario/component.tsx`.
