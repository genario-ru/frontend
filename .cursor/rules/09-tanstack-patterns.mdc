---
description: TanStack Query, Router, and Form usage patterns
alwaysApply: true
---

# TanStack Patterns

## TanStack Query

Generated query/mutation hooks come from `@/codegen/api/product`. Prefer action
wrappers for network behavior used by widgets/features:

```typescript
import { useGetTemplates } from "@/actions/templates/hooks/use-get-templates";
```

GET requests should be handled through TanStack Query state (`data`,
`isLoading`/`isPending`, `isError`, `error`, `refetch`). Do not add
`try/catch` around query hooks.

## Preloading in route guards/loaders

```typescript
import { getApiV1AuthSessionQueryOptions } from "@/codegen/api/product";

export const Route = createFileRoute("/_with-auth")({
  beforeLoad: async ({ context }) => {
    await context.queryClient.ensureQueryData(
      getApiV1AuthSessionQueryOptions(),
    );
  },
});
```

## Infinite queries

```typescript
import { useGetApiV1CreditsUsageMyInfinite } from "@/codegen/api/product";

const { data, fetchNextPage, hasNextPage, isFetchingNextPage } =
  useGetApiV1CreditsUsageMyInfinite({ pageSize: 20 });
```

Wrap this in an action hook before exposing it to widgets/features unless there
is a clear reason to keep it local.

## Mutations

```typescript
import { usePostApiV1Profiles } from "@/codegen/api/product";

const { mutate, isPending } = usePostApiV1Profiles();

mutate(
  { data: { name: "Profile" } },
  {
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey });
    },
  },
);
```

Use mutation callbacks for backend-request side effects:

- common side effects belong in the action hook's `mutation.onSuccess`,
  `mutation.onError`, and `mutation.onSettled`;
- UI-flow-specific side effects belong in the `mutate(variables, callbacks)`
  call from a widget hook;
- avoid `mutateAsync` plus `try/catch` unless the user approved a custom flow.

## Router search params

```typescript
import { useNavigate, useSearch } from "@tanstack/react-router";

const search = useSearch({ from: "/_with-auth/_with-subscription/home" });
const navigate = useNavigate();

navigate({
  to: "/home",
  search: (prev) => ({ ...prev, q: "new" }),
  replace: true,
});
```

## Router params

```typescript
import { useParams } from "@tanstack/react-router";

const { scenarioId } = useParams({
  from: "/_with-auth/_with-subscription/scenarios/$scenarioId",
});
```

## TanStack Form

Always use `useAppForm` from `@/lib/tanstack-form`:

```typescript
import { useAppForm } from "@/lib/tanstack-form";
import { z } from "@/lib/zod";

const form = useAppForm({
  defaultValues: { email: "", name: "" },
  validators: {
    onSubmit: z.object({ email: z.email(), name: z.string().min(1) }),
  },
  onSubmit: async ({ value }) => {
    // mutation call
  },
});
```

Common field components include `InputField`, `TextareaField`, `SelectField`,
`CheckboxChipsField`, `MultiSelectField`, and `RadioCardsGroupField`.
Common form components include `SubmitButton` and `ResetButton`.

Large forms may be split with `withForm`, but form parts stay inside the owning
widget. Do not move `withForm` children into `src/features/**`.

## Reference examples

- GET action hook: `src/actions/templates/hooks/use-get-templates.ts`.
- Mutation action hook with callbacks:
  `src/actions/ideas-lists/hooks/use-create-ideas-list.ts`.
- Per-call mutation callback from a widget hook:
  `src/widgets/scenario/scenario-app-menubar/hooks/use-scenario-app-menubar-save.ts`.
- Widget-owned TanStack Form:
  `src/widgets/profile-settings/profile-settings/hooks/use-profile-settings-form.ts`.
- `withForm` subcomponents:
  `src/widgets/profile-settings/profile-settings/components/profile-settings-form-fields.tsx`
  and
  `src/widgets/profile-settings/profile-settings/components/profile-settings-form-buttons.tsx`.
