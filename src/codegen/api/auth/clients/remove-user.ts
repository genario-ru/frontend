/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/api/client";
import fetch from "@/lib/api/client";

import type {
  RemoveUser400,
  RemoveUser401,
  RemoveUser403,
  RemoveUser404,
  RemoveUser429,
  RemoveUser500,
  RemoveUserMutationRequest,
  RemoveUserMutationResponse,
} from "../models/remove-user.ts";
import {
  removeUserMutationRequestSchema,
  removeUserMutationResponseSchema,
} from "../zod/remove-user-schema.ts";

function getRemoveUserUrl() {
  const res = { method: "POST", url: `/api/auth/admin/remove-user` as const };
  return res;
}

/**
 * @description Delete a user and all their sessions and accounts. Cannot be undone.
 * {@link /admin/remove-user}
 */
export async function removeUser(
  { data }: { data: RemoveUserMutationRequest },
  config: Partial<RequestConfig<RemoveUserMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = removeUserMutationRequestSchema.parse(data);

  const res = await request<
    RemoveUserMutationResponse,
    ResponseErrorConfig<
      | RemoveUser400
      | RemoveUser401
      | RemoveUser403
      | RemoveUser404
      | RemoveUser429
      | RemoveUser500
    >,
    RemoveUserMutationRequest
  >({
    method: "POST",
    url: getRemoveUserUrl().url.toString(),
    data: requestData,
    ...requestConfig,
  });
  return removeUserMutationResponseSchema.parse(res.data);
}
