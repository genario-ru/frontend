/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@/lib/api/utils/client.ts";
import fetch from "@/lib/api/utils/client.ts";

import type {
  RevokeUserSession400,
  RevokeUserSession401,
  RevokeUserSession403,
  RevokeUserSession404,
  RevokeUserSession429,
  RevokeUserSession500,
  RevokeUserSessionMutationRequest,
  RevokeUserSessionMutationResponse,
} from "../models/revoke-user-session.ts";
import {
  revokeUserSessionMutationRequestSchema,
  revokeUserSessionMutationResponseSchema,
} from "../zod/revoke-user-session-schema.ts";

function getRevokeUserSessionUrl() {
  const res = {
    method: "POST",
    url: `/api/auth/admin/revoke-user-session` as const,
  };
  return res;
}

/**
 * @description Revoke a user session
 * {@link /admin/revoke-user-session}
 */
export async function revokeUserSession(
  { data }: { data: RevokeUserSessionMutationRequest },
  config: Partial<RequestConfig<RevokeUserSessionMutationRequest>> & {
    client?: Client;
  } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const requestData = revokeUserSessionMutationRequestSchema.parse(data);

  const res = await request<
    RevokeUserSessionMutationResponse,
    ResponseErrorConfig<
      | RevokeUserSession400
      | RevokeUserSession401
      | RevokeUserSession403
      | RevokeUserSession404
      | RevokeUserSession429
      | RevokeUserSession500
    >,
    RevokeUserSessionMutationRequest
  >({
    method: "POST",
    url: getRevokeUserSessionUrl().url.toString(),
    data: requestData,
    ...requestConfig,
  });
  return revokeUserSessionMutationResponseSchema.parse(res.data);
}
