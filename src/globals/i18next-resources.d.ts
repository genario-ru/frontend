interface Resources {
  translation: {
    common: {
      days_count: {
        days_few: "{{count}} days";
        days_one: "{{count}} day";
        days_other: "{{count}} days";
      };
    };
  };
}

export default Resources;
